/*why we use npm -- because react is so powerful and we have to minimize bundle as well as optimize app
and those helper packages come inside npm*/

//caret^ is use so that we have updated version if we dont use thn it is stick to given version

//here package-lock tell us that which version we r actually using(it will actually lock the  version)


